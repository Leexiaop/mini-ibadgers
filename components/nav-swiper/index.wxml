<!--components/nav-swiper.wxml-->
<t-swiper
	autoplay="{{true}}"
	duration="{{500}}"
	interval="{{5000}}"
	height="{{height + 'rpx'}}"
	navigation="{{navigation}}"
	paginationPosition="{{paginationPosition}}"
	list="{{list}}"
>
</t-swiper>